#!/usr/bin/env bash
#Bash script that displays the
#time for 12 hours and 59 minutes
x=0
while [ $x -le 12 ]
do
    echo "Hour: $x"
    i=0
    while [ $i -le 60 ]
    do
        echo $i
        i=$(( i + 1 ))
    done
    x=$(( x + 1 ))
done
